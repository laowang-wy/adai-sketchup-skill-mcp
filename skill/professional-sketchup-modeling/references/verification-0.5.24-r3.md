# 验证边界：0.5.24-r3-su2019-20260919

这是增量工程候选，仍不能声明“稳定底座”或 E00—E22 全部完成。

实际 SU2019 19.0.685 / Ruby2.5.1 使用隔离工程试件完成：

- 四阶段独立实体、实际审计与多视图截图、显式审查、最终保存。
- 已提交建模步骤丢回包 → 原回执与当前模型提交标记核对 → retry_evidence；没有重建几何。
- 内部组平移、隐藏、组材质三种变化：外包围盒不变，旧证据均被拒绝；原场景不变时通过。
- 合法镜像实例通过；错位、错误 Definition、删除实例失败。预期放置同时绑定祖先路径和世界变换。
- JSON 冒充已看图片被拒；不带质量通过报告的明确 revise 可以执行。
- 同文档多项目合法构造通过；阶段删除已执行但回包丢失时，同项目重放和另一项目写入均被阻止。
- 内部 patch 的合法平移、显式保护拒绝、回退、重新取证、审查及保存执行过。保存结果在另一 SU 进程重开：24 个面、8 个组，实际编辑与撤销通过。

实机暴露并修复：Ruby2.5 的 File.open('wx') / filter_map 不兼容、细部相机向量平行、不同保护条件预览 ID 冲突，以及工具自身切换项目可见性导致新摘要误拒绝。可见性归一化仅限工具拥有的项目根；内部对象隐藏检查仍保留。

未完成项：

- H04：建模步骤回执恢复已实测；阶段删除、保存和 patch 的完整跨动作回执恢复尚未闭合。结果未知保留阻止写入状态，不允许人工改签名状态过关。不同 APPDATA 状态根的跨宿主协调仍未验收。
- H06/H07：patch 在正常 MCP 工具发现和执行中关闭；内部实现仍需完整共享路径／依赖写集和未知结果恢复测试。
- H08：屋盖配方公开 validate/compile 与非法尺寸离线用例有结果；配方到真实屋盖来源建模的完整链条为 not_run。
- 建筑来源匹配、复杂整栋质量、SU2018、新机器、双模型能力与费用对照、宿主原始用户输入真实性验证均为 not_run。
- 原离线套件保留，不修改旧预期。其中旧 test_platform 要求全局环境变量选择 autonomous，与追加通知的新任务默认规则冲突；保留该失败记录，另有实际 stdio 模式正反例验证新规则。

手动模式为辅助偏好：新任务 guided，用户明确选择后 autonomous；不改变检查、权限和保护。宿主未经修改，本功能不证明用户输入真实性，也不提供许可证或防复制认证。

详细测试源码、原始失败日志、实机图像、SKP、命令和安装包哈希随安装 ZIP 外的研发验证包交付。每份证据标注自己的运行版本与范围；安装包的空状态启动检查不等于实机验收。

