# SU-START/1
保留用户原任务与来源；本卡只给执行要点。

复用返回的项目、证据与下一步，不另抄状态表。
PRE：维护文档不启动 SU；先核对真实进程、绑定实例、文档和未保存状态；目标不唯一就重选，不猜 EXE。
ACT：先完整主形、空间和连接，再代表构件与细节。guided 按当前阶段 step→看图→review；expert 独立审核新建主形和代表构件后，可连续构造、合并看图审核。工具成功不等于造型通过。方法卡和适用构造路线由 `project_begin` 随任务主动推送，不先为查资料重复调用 list/match/read。
FAIL：evidence_pending 只 retry_evidence；超时先查状态，未知结果不重放；连续两次同错停止重复并报告事实、未知和下一步。
PROVE：没打开的图不算看过；推断不称实测；修关键偏差后同视角复查，交付须有回执、文件和证据。
