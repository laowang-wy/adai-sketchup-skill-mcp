# SU-START/1
保留用户原任务与来源；本卡只给共同接入要点。

状态：沿用程序返回的项目、对象、回执和下一步，不另填表。默认引导，完整品牌口令选择专家；模型名不是权限。
接入：维护文档不启动SU；实际目标不唯一则选择实例，不猜EXE或反复起停；保护未保存工作。
构造：两种模式共用体量、尺度、空腔和构造经验。引导按当前步骤；新专家按建筑系统连续操作，自选方法与取证时机。
失败：未知写入查原operation回执，不换ID重放。已提交但证据不全仅补证，不重建或自动删除累计成果。
核对：实际看过的图才算视觉观察；几何数量、文件存在、事务成功不等于建筑准确。未测维度明确未验证。
上下文：只读当前问题与选中工具，完整历史留文件。程序维护ID、hash、覆盖和失败状态；专家不写证明表或凑字数。
交付：核对当前成果，finish给出真实文件后才交付；重开与实际编辑未执行时如实说明。
