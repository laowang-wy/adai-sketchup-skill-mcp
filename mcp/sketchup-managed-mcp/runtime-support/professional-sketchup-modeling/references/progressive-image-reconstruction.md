# Progressive image reconstruction

Use the managed phase sequence to preserve source evidence, register only observed subjects, and request fresh evidence after a capture or model change. If evidence generation fails, keep the geometry checkpoint and use the managed recovery action; never replay an uncertain write.
