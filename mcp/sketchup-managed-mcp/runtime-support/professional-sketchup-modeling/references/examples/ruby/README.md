# Ruby examples

These examples are reference snippets only. Production SketchUp execution stays inside the managed transaction and uses the registered managed project entry points.

`ancient-construction-patterns.rb` is the portable subset of the original curved-corner, roof-end, dougong-datum and single-bay support-chain work. It is a construction helper, not a source-specific preset or a quality gate. Read it only when the current source needs those relationships, and supply measured stations from current evidence.
