# Ancient gates

Treat gates as source-visible primary and unique geometry. Query the named semantic phase through the managed readback path, preserve the active document, and use a custom managed route when the available recipe does not apply.
